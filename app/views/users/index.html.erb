<% title "Membres | " %>

<h1>Tous les membres</h1>

<%= will_paginate %>

<ul class="users">
    <% @users.each do |user| %>
    <p></p>
    <%= link_to user.username, user %>
    <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "(supprimer)", user, method: :delete, data: { confirm: "Vous êtes sûr ?" } %>
    <% end %>
    <% end %>
</ul>

<%= will_paginate %>
